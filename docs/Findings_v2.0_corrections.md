# Findings v2.0-Corrections — 独立深探整改全记录（R1+R2 实锤的逐项处置）

> 2026-08-29。独立深探：reviews/deep_probe_R1.md（科学主张线）、
> reviews/deep_probe_R2.md（工程复现线）。登记：docs/SPEC_v2.0_amendment1.md。
> 本文是诚实性中枢：每项更正给出「旧表述 → 实锤证据 → 新表述」。
> 原则：冻结文件不回溯改写；实验代码的 bug 修正后重跑并记录修正史；
> 文档级错误以修正案与本文更正，不粉饰既往。

## C1 斩杀线触发：H-A 头条主张正式判死（最高严重度）

- **旧**：Findings_v2.0.md「H_A_dead：未触发（H-A1 显著；反转仅 1 张 <3）」。
- **实锤**：冻结结果 JSON `H_A_dead.triggered=true`（20 图 3 反转、22 图 4 反转：
  L_historical/L_physics/L_project_management/S2_n45）；「16/20」与
  15+/3−/2= 的文数不符；verifier 全版本从未检查 kill_lines 字段。
- **新**：按预登记析取规则（不显著 **或** ≥3 反转），**H-A（vs random）判死**。
  收缩后存活：① field_mean > degree 跨口径稳健（Holm 过；R1 代表 10 张
  p=0.0078）；② 高 hub 结构图的局部优势（S6 族 0.471）；③ 反转图全部位于
  语义/低枢纽图——**斩杀线触发本身就是分工边界的直接证据**，与 H-B1 违规、
  PoA<1 图同构。22 图口径：H-A1 p=0.0118（Holm 过）与斩杀线并存，
  以预登记斩杀线为准，headline 一律标注判死状态。
- 教训制度化：此后 Findings 的「未触发/触发」表述必须由脚本从结果 JSON
  机械读取，禁止手写（已列入 verifier v21 检查项）。

## C2 「先验=CoT」声称撤回

- **旧**：boss.md「先验在更难的信息通道上达到直接问答水平（92.5%=92.5%）」。
- **实锤**：陷阱选项非图节点 ⇒ 先验有效任务 39/40 题退化为 2 选 1；
  开放候选实测先验 top-1=67.5%、top-3=82.5%；2 选 1 期望 94.4% 完全解释观测。
- **新**：声称撤回。可比口径：CoT 92.5%（带选项文本的 4 选 1）vs
  先验开放 top-1 67.5% / top-3 82.5%；field_mean 题库 52.5% 记为
  2 选 1 机会水平（不携带信息）。

## C3 BOSS 门槛与 tfidf 抽签披露

- **实锤**：tfidf 在族 S 标签脱钩图上余弦近全零（S4 金边 >0 仅 0/58），
  排序由 tiebreak 抽签主导；S5 胜场 37.5% 抽签可复现（margin=1 边）；
  生成器无 bug。
- **新**：门槛 margin ≥3 金边。门槛后 BOSS 6 事件（L_hist-CN、L_phys-tfidf、
  L_PM 三臂、S4-tfidf）；被拦 4 事件（含 S5）降级为抽签候选并如实保留。
  附带更正：boss.md 附注「n=20」张冠李戴（S5 实为 n=7 的 named 子集）。
- 新 BOSS 值得注意：**L_project_management 三臂（PA/n2v/tfidf）同图击败场**
  （margin 6 边）——该图结构（单 hub 24 浅星型）场的路径条件化失效，
  列入 v2.1 重点剖析对象。

## C4 光子 P2 更正

- **旧**：14/22 可探测、「>14 跳耗尽 SNR」、「8 图 >40 dB」。
- **实锤**：NEP 单位 bug（下限严 10⁶ 倍）+ max_hops=10 截断 + 索引贪心。
- **新**：真最长路径 DP + 单位修正 ⇒ **18/22 可探测，阈值 ≈27 跳**；
  不可探测 = L_algorithm_process(29 跳)、S1_n35(34)、S1_n45(44)、S1_n60(59)；
  「8 图 >40 dB」表述撤回（实 23–26 dB）；「跨层互证」降级为「方向一致」。
  （注：结果 JSON 的 note 已数据化生成并写入修正史。）

## C5 工程整改（R2/E1–E5）

- **E1 冷启动**：run_v17_fusion_fix.py 未推送（6 脚本 import 它）→ 本批补推；
  MANIFEST 语料快照承诺修正（20→22 图使 p 0.0075→0.0118，「确定性再生」
  对语料版本不成立，后续以 index sha256 钉快照）。
- **E2 verifier 漂移**：v12/v18 计数断言改下界；v17/v20 受实验更正影响的
  锚点走 erratum（冻结不覆写，v21 承接）；后续新检查一律断言结构字段。
- **E3 index 泄漏**：根因 = 驱动直调 ingest_domain 未建索引（41 分钟静默漏图）；
  修复 = 摄入即建索引 + load_corpus 孤儿哨兵（显式报错）。
- **E4 盲区**：crossval/bigquiz/photonics 零覆盖 → 补测清单并入 v2.1 待办。
- **E5 预算**：未登记 ~29 次已追认登记（修正案 A5）；PM 单 prompt 6 次尝试
  登记为预算例外；新规 = attempts 必须落盘且禁止幂等擦除。

## D. 深探方法论复盘（为何这次抓得准）

两个探针共 9 问全部产生实锤级更正，且 R1/R2 独立撞上同一处斩杀线矛盾——
说明：(i) 「文档手写 vs 数据机械」是最富矿的 bug 类别（本项目已三次中镖：
Table C1、kill_lines、16/20）；(ii) 单位与截断是第二富矿（NEP、max_hops）；
(iii) 冻结数据可信、散文不可信——此后的规矩是 **Findings 数字必须可追溯
到具体结果 JSON 的具体字段**，verifier 增加 kill_lines 类字段的强制对照。
