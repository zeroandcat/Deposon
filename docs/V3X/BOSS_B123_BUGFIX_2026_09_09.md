# BOSS-B1/B2/B3 字段名 Bug 修复报告

**日期**: 2026-09-09
**Worker**: session mvs_36769d4464384d4bbad3c5d100e4f6ef
**触发 brief**: docs/V3X/BOSS_SELFTEST_PHASE_B_2026_09_09.md §1

---

## 1. 改动文件 + SHA-256 对比

| 文件 | 改前 SHA-256 前 12 | 改后 SHA-256 前 12 | diff 行数 |
|---|---|---|---|
| `.mavis/scripts/kt_b1/boss_b1_sinkhorn_ot.py` | `19325960B8BE` | `EC18DE386FF5` | 3 行 |
| `.mavis/scripts/kt_b1/boss_b2_kd.py`        | `1781EA2F742D` | `2C33C5395DF0` | 2 行 |
| `.mavis/scripts/kt_b1/boss_b3_llmlingua.py` | `C0B55E0385A4` | `51B57F13A731` | 4 行 |

3 个 `.bak` 备份保留在同目录(`boss_<n>_*.py.bak`),可对照回滚。

### diff 摘要(全部 "predicted" → "pred",字段名层面)

**B1**:
- L96 docstring output 字段描述:`"predicted": float` → `"pred": float`
- L114 dict 写入+读取:`"predicted": float(p.get("predicted", 0.0))` → `"pred": float(p.get("pred", 0.0))`
- L159 dict 读取:`pred = question.get("predicted", 0.0)` → `pred = question.get("pred", 0.0)`

**B2**:
- L95 dict 写入+读取:`"predicted": float(p.get("predicted", 0.0))` → `"pred": float(p.get("pred", 0.0))`
- L139 dict 读取:`q.get("predicted", 0.0)` → `q.get("pred", 0.0)`

**B3**(耦合最紧,format 字符串 + 正则 + dict read 三处同步):
- L88 dict 写入+读取:同上
- L108 docstring:`condition=<condition>, predicted=<predicted>, answer=<answer>` → `condition=<condition>, pred=<pred>, answer=<answer>`
- L116 format 字符串:`f"predicted={question['predicted']}, "` → `f"pred={question['pred']}, "`
- L146-147 正则+format:`r"predicted=(-?\d+\.?\d*)" / f"predicted={q['predicted'] + 0.1 * i:.4f}"` → `r"pred=(-?\d+\.?\d*)" / f"pred={q['pred'] + 0.1 * i:.4f}"`

剩余 5 处 "predicted" 均为描述性注释(`基于 predicted / best_path`、`按 predicted 强度`),非字段访问,按"只改字段名"约束保留。

---

## 2. 自测结果(3/3 仍 FAIL,但根因已变)

| 脚本 | 改前错误 | 改后错误 | 字段名 bug 状态 |
|---|---|---|---|
| boss_b1_sinkhorn_ot.py | `TypeError: float() arg must be ... not 'NoneType'` | `ValueError: could not convert string to float: 'No'` | ✅ 已修(None → 拿到真实值) |
| boss_b2_kd.py          | `TypeError: float() arg must be ... not 'NoneType'` | `ValueError: could not convert string to float: 'No'` | ✅ 已修 |
| boss_b3_llmlingua.py   | `TypeError: float() arg must be ... not 'NoneType'` | `ValueError: could not convert string to float: 'No'` | ✅ 已修 |

3 个脚本退出码均=1,均未输出主指标(Sinkhorn 失真上界 / KD 失真上界 / LLMLingua 失真率)。

### 字段名 bug 修复证明(从 gsm8k 路径走查)

- **改前**: `p.get("predicted", 0.0)` 拿不到 key(gsm8k 用的是 `predicted`,strategyqa 用 `pred`,混合),fallback 0.0/None 流入下游 → TypeError
- **改后**: strategyqa 路径能拿到 `pred` 的真实值(字符串 "Yes"/"No");gsm8k 路径仍 fallback 0.0(因为 v19 frozen 里 gsm8k 用的是 `predicted` 字段,且值为 None)

**字段名 bug 100% 修复**(None 不再来自 fallback,strategyqa 真实值现在能取到)。**未达到 brief 期望的 PASS**,原因见 §4 blocker。

---

## 3. 5 锚 JSON 验证

```
锚文件: verifier/handoff/KT_ABC1_anchors_sha256_12.json
改前 sha256 前 12: 03C6C01F3697
改后 sha256 前 12: 03C6C01F3697  ← 完全一致
```

`boss_baselines` 区块里 BOSS-B1/B2/B3 的 `sha256_12` 字段也仍是 `19325960b8be` / `1781ea2f742d` / `c0b55e0385a4`(这些是**改前**的 sha,本次修改未触及锚文件)。

---

## 4. Blocker / 风险(必须 parent 决策)

**新发现的 v19 frozen JSON schema/数据问题**(超出"只改字段名"worker 范围):

1. **Schema 不一致**:v19 frozen 的 200 题里,gsm8k 部分字段名是 `predicted`(值=None),strategyqa 部分字段名是 `pred`(值=字符串 "Yes"/"No")。**不是 brief 假设的"统一用 pred"**。
2. **gsm8k 数据缺失**:`predicted` 字段全部为 `None`,说明 v19 frozen benchmark 的 gsm8k 部分从未跑过 LLM,只填了 schema 框架。
3. **strategyqa pred 是字符串**:yes/no 任务的 `pred` 存为字符串,不能直接 `float()`。需新增 `parse_pred(v) -> float` 函数处理 "Yes"=1.0 / "No"=0.0。

**自测期望未达成**:3/3 仍 FAIL,但根因已从"字段名 NoneType"变为"schema 字符串/缺失"。字段名层面 100% 修复完成。

**worker 建议给 parent**:
- 3 个 BOSS 脚本的字段名修复可保留(已正确)
- 需另起一个 worker 任务处理 v19 frozen JSON 的 schema 统一 + 数据填充
- 范围包括:(a) gsm8k/strategyqa 字段名统一为 `pred` 并填 LLM 跑过的真实 float 值;(b) BOSS 脚本里 `float(p.get("pred", 0.0))` 改为 `parse_pred(p.get("pred", 0.0))` 处理 yes/no 字符串

**worker 临时副产物**: `.mavis/scripts/kt_b1/_inspect_v19_pred.py`(694 字节,worker 调试用,读取 v19 schema)。删除工具不可用(mavis-trash 在 Windows 未部署,Remove-Item 被安全策略阻止),parent 可手动清。

---

## 5. 5 项严格约束 自查

| 约束 | 状态 |
|---|---|
| 只改 3 个 BOSS 脚本,每个只改字段名 | ✅ (9 处 dict 访问) |
| 不改 5 锚 JSON | ✅ 哈希未变 |
| 不改 4 SPEC V0.1 冻结版 | ✅ 未触碰 `docs/V3X/KT_?1_SPEC_V0.1.md` |
| 改完立即验 3 次自测 | ✅ 跑过,结果记录在 §2 |
| 备份原文件 .bak + diff | ✅ 3 个 .bak 在同目录,§1 给了 diff 摘要 |
| 不调用 LLM API | ✅ 纯本地 self_test |

---

**交付完成**。3 个 BOSS 脚本字段名 bug 修复,worker 任务边界内全部完成;v19 schema/数据问题已作为 blocker 透明上报 parent。
